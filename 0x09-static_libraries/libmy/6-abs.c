#include "main.h"

/**
  * _abs - Calculated absolute value
  *
  * Return: Absolute value
  * @n: takes an int values
  */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (n * -1);
	return (n);
}
